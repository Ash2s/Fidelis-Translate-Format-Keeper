# 翻译工具去重与保真修复方案

> 配套文档：《翻译工具问题类型总结与修复建议.md》（badcase 源文档，47 项问题）
> 适用范围：Windows 版 / macOS 版（两版代码一致，方案统一适用）
> 方案性质：**判定分级 + 白名单保护 + 中英对照校验 + 删除审计** 四层闭环，替代现有"无差别盲删"
> 执行策略：分级执行——高置信度自动删 / 中置信度告警 / 低置信度保留

---

## 〇、问题定位与方案总览

### 0.1 核心矛盾

badcase 类型 2（真重复：`Cai Yuan Cai Yuan`、`V1.0 V1.0`、`Original Acquisition Original Acquisition` 等）必须去重；而**专有名词、补充说明、语境差异、引用段落**等合法内容，被同一套去重逻辑误删。二者目前没有任何区分机制。

### 0.2 三处误删源头（代码级定位，已核实）

| # | 文件 / 函数 | 现状逻辑 | 误删机制 | 危险等级 |
|---|---|---|---|---|
| 1 | `translator.py::_clean_segment` | 短语级去重正则 `(\S+(?:\s+\S+){0,6})\s+\1\b`，贪婪捕获 1~7 token，后跟相同序列即删；另有 `\b(\w+)\s+\1\b` 词级去重 | ① 无法区分停用词/内容词/专名；② 数字 token 无保护（代码注释自认需先跑日期清理规避）；③ 无审计，删了不可追溯 | **高** |
| 2 | `document_parser.py::apply_per_run_formatting` | 相邻 run：全文相等 → 清空后 run；`prev末词 == curr首词`（**大小写不敏感**）→ 删 curr 首词 | ① 专名 `Software` / `software` 被大小写折叠误判；② 跨 run 专名（`Software` + `Software Copyright`）首词被删，专名残破；③ 整 run 清空连带丢失该 run 的加粗/高亮格式 | **高** |
| 3 | `translator.py::polish_text` | system prompt 要求删除 "Duplicate / repeated words"，无任何限定 | LLM **语义去重不可控**：并列结构、补充说明、语义近似句被当作"重复"删除——"大量非重复内容被移除"的最大嫌疑源 | **高** |

### 0.3 误删的本质

现有去重是"**无上下文、无白名单、无验证、无日志**"的四无盲删。判定维度只有字符串相似，缺少四个关键信息源：

1. **中文原稿对照**——翻译流水线中每个段落都知道其中文原文（`routes.py::_translate_paragraphs_sync` 已确认）。英文重复了，中文重复了吗？
2. **词频 / 稀有度**——专有名词在全文档低频出现，本就不该参与去重。
3. **上下文**——被删片段的前后 token 是否与它构成专名 / 编号 / 引用。
4. **审计**——每次删除都应记录位置、原因、置信度，可回溯可复核。

### 0.4 方案总览（四层闭环）

```
┌─────────────────────────────────────────────────────────────────┐
│  第 1 层  判定    多级判定器（精确 → 归一化指纹 → 编辑距离 → 语义）   │
│          每个候选重复输出：位置 / 类型 / 置信度                      │
├─────────────────────────────────────────────────────────────────┤
│  第 2 层  分级    高置信度 → 自动删；中置信度 → 告警；低置信度 → 保留 │
├─────────────────────────────────────────────────────────────────┤
│  第 3 层  保护    三重门触发 + 白名单：停用词 / 数字 / 专名 / 编号    │
│                    / URL / 邮箱 / 引用段落 / 作用域限定              │
├─────────────────────────────────────────────────────────────────┤
│  第 4 层  对照+审计   CN↔EN 双向保真校验（数字/术语/专名完整性）      │
│                    删除审计日志 → 汇入质检报告                      │
└─────────────────────────────────────────────────────────────────┘
```

**总原则**：默认保留，只有同时满足"紧邻 + 精确 + 达到长度阈值 + 不命中白名单"时才自动删除；任何删除都留下审计记录；删除前后用中文原稿做保真校验，发现信息缺失立即告警。

---

## 一、重复判定标准（精确判定 + 触发条件）

### 1.1 判定对象与作用域

| 维度 | 规则 | 理由 |
|---|---|---|
| 判定粒度 | token 序列（按空白切分，`\w+'?` 归一化） | 与现有代码一致，避免字节级误判 |
| **作用域** | **仅段落内（单 `w:p`）与字段内（单表格单元格）** | 标题与正文的合法互引、不同段落重复出现的机构名一律不判重 |
| 跨段判定 | 一律禁止（含同文档、跨文件） | 专名/术语在不同位置重复出现是正常现象 |
| run 边界 | 段内 run 先无缝拼接成完整文本，再判定 | 消除"run 拆分导致的伪重复"与"run 边界插入空格"两类源头（对应类型 1/7） |

### 1.2 多级判定器（按顺序逐级判定，取最高置信度）

**L1 精确匹配（字符级）**
- 定义：相邻 token 序列完全一致，`token[i] == token[i+k]`（大小写敏感）。
- 用途：真重复检测（复制粘贴型），badcase 类型 2 主检测器。
- 扫描算法：对文本 token 化后，对每个起始位置 i，枚举重复长度 k = 1..8，检查 `tokens[i:i+k] == tokens[i+k:i+2k]`，取**最长匹配**（避免嵌套误判）。
- 实现：见 `dedup_guard.py::scan_exact_adjacent`。

**L2 归一化指纹（词级）**
- 定义：token 小写 + 去标点 + 去停用词后相同；或 shingle（2~3-gram）Jaccard 相似度 ≥ 0.9。
- 用途：检测大小写漂移 / 标点差异造成的"视觉重复"（如 `Ruanzhudengzi` / `Ruan Zhu Deng Zi`）。
- **仅告警，不自动删**（涉及专名漂移，需人工确认，对应 badcase 类型 8）。

**L3 编辑距离（字符级）**
- 定义：Levenshtein 距离 ≤ 2 且长度差 ≤ 20%。
- 用途：检测"疑似截断"（`288 yuan` vs `88 yuan`、`10,250,0.00` vs `10,250,000.00`）。
- **仅告警**（这类是数字截断，应修提取层，不是去重——对应 badcase 类型 1）。

**L4 语义相似度（可选，模型级）**
- 定义：embedding 余弦相似度 ≥ 0.95。
- 用途：检测 polish 模型制造的跨句语义重复。
- **仅告警**，且默认关闭（成本高、误报率高），仅在质检人工复核时按需启用。

### 1.3 触发条件（三重门，必须全部满足才自动删除）

| 门 | 条件 | 参数（可配置） |
|---|---|---|
| **邻近性门** | 重复片段紧邻，中间最多 1 个标点/空白（无其他实词） | 间隔 ≤ 1 token |
| **长度门** | 重复片段 ≥ 2 个实词（过滤停用词后计数），或整体长度 ≥ 6 字符 | 实词 ≥ 2 或长度 ≥ 6 |
| **置信度门** | L1 精确匹配且不命中白名单（见第二章） | L1 + 白名单 |

**单 token 例外规则**（k = 1 时）：
- token 为**停用词**（the/a/an/of/and/in/on/for…）→ 自动删（`the the` 一定是笔误）；
- token 为**数字 / 版本号 / 编号**（`V1.0`、`2025`、`17558889`、`L-04077`）且紧邻精确重复 → 自动删（`V1.0 V1.0` 一定是拼接错误，badcase 类型 2 要求删）；
- token 为**其他实词**（`Cai Cai`、`Software Software`）→ 仅告警，不自动删（可能为专名）。

### 1.4 与现有正则的差异对照

| 场景 | 现有正则行为 | 新判定器行为 |
|---|---|---|
| `the the` / `of of` | 删 | 删（停用词例外） |
| `V1.0 V1.0` | 删 | 删（编号例外） |
| `Cai Yuan Cai Yuan`（2 token） | 删 | 删（L1，实词≥2） |
| `Cai Cai`（1 token 实词） | 删 | **告警保留**（可能专名） |
| `Software` / `software`（大小写） | 删（`lower()` 折叠） | **告警保留**（L2 仅告警） |
| `2025 2025.10.13`（日期残留） | 依赖前置日期清理 | 同前，日期清理仍最先执行 |
| 标题与正文共享短语（跨段） | 不匹配（不同段分别处理） | 不判重（作用域限定） |

---

## 二、去重算法 / 策略与保护机制

### 2.1 三种去重策略的落位

| 策略 | 落位 | 说明 |
|---|---|---|
| **增量比对** | 流水线层：逐段（paragraph / 单元格 / 文本框）独立处理，段内建立 `(cn_text, en_text)` 映射 | 每段翻译时只与**自己的中文原稿**比对，天然杜绝跨段误删 |
| **指纹去重** | `_clean_segment`：段内 token 流做 shingle 指纹，只处理**紧邻重复**（指纹相同且位置相邻） | 复制粘贴型重复的特征就是紧邻 |
| **上下文感知** | `apply_per_run_formatting`：删除前检查被删 token 的前后 token | 若与前/后 token 构成专名 / 编号 / 版本号（如 `Software` + `Copyright`），跳过删除 |

### 2.2 保护 / 白名单机制（删除前逐项检查，命中任一 → 保留或降级为告警）

| # | 保护项 | 判定方式 | 降级为 |
|---|---|---|---|
| 1 | **长度阈值** | 实词数 < 2 且非停用词 / 编号 | 告警 |
| 2 | **停用词** | token ∈ 停用词表（约 30 词） | 自动删（唯一自动删的 k=1 场景） |
| 3 | **稀有词 / 专名** | 全文档词频统计，出现 ≤ 2 次的 token 加入保护集合 | 告警 |
| 4 | **数字 / 金额** | `\d[\d,.]*`（含千分位/小数点） | 仅精确完整重复时可删；模糊/截断告警 |
| 5 | **编号 / 版本号 / 证书号** | `[A-Z]{1,4}[-.]?\d[\d.-]*`、`V\d+(\.\d+)+`、`软著登字第` 对应英文模式 | 仅精确完整重复时可删 |
| 6 | **URL / 邮箱** | `_URL_LINE_RE`、`\S+@\S+\.\S+` | 已有 URL 分段保护保留；邮箱 token 加保护 |
| 7 | **方括号标记** | `\[(Image|Seal|Barcode|Logo|Original)\]` | 永不参与去重（译前已注入，翻译/润色/去重全程保护） |
| 8 | **引用段落** | 以引号开头/结尾，或含引号包裹的 3+ 词短语 | 永不参与去重 |
| 9 | **术语表命中词** | 《蔡远_专有术语表_最终版.xlsx》命中的 CN→EN 对 | 译后不参与去重（术语一致性优先，对应类型 8） |
| 10 | **作用域** | 段内 / 字段内 | 跨段一律不判重（见 1.1） |

**保护优先级**：保护命中 → 该候选**自动删除被否决**，降级为告警（写入质检报告），绝不静默保留或静默删除。

### 2.3 中英对照双向校验（保真核心）

**原理**：翻译时每个段落已知中文原稿，去重/后处理**之前和之后**各做一次对照，凡英文内容缺失而中文存在 → 拦截。

| 校验项 | 规则 | 级别 |
|---|---|---|
| 数字完整性 | CN 提取全部数字（含单位换算：万=×10⁴、亿=×10⁸），与 EN 数值集合比对，缺一告警 | 硬（badcase 类型 1） |
| 术语完整性 | 术语表命中词的标准译法必须出现在 EN | 中（类型 8） |
| 专名完整性 | 人名/机构名/证书号（拼音库+术语表）必须出现 | 中（类型 6/8） |
| 段落对齐 | CN 段落数 == EN 段落数，段内句子数误差 ≤ 1 | 中 |
| **删除审计** | 每次去重记录 `{段落, 位置, 被删片段, 判定类型, 置信度, 中文对照}` | 硬（人工可复核） |

**双向逻辑**：
- CN 无重复 + EN 重复 → **修源头**（run 拼接 / 字段填充），**禁止删英文**；
- CN 有重复 + EN 重复 → 真重复，允许按判定器删除；
- CN 有内容 + EN 缺失 → 保真告警（数字/术语/专名校验拦截）。

---

## 三、修复步骤清单（含优先级）

> 每步标注：输入 → 处理逻辑 → 预期输出。P0 先做（误删主源、风险最高），P1 其次，P2 为闭环增强。

### P0-1 段内 run 无缝拼接（修伪重复源头）

- **文件**：`document_parser.py::_get_element_text`、`extract_paragraphs`、`extract_table_cells`、`extract_textbox_paragraphs`
- **输入**：docx 段落内多个 `w:r` / `w:t`
- **处理**：同一 `w:p` 内所有 `w:t` 文本**直接拼接，不插入任何空格**；空格只来自 `w:t` 内真实空格与 `w:tab`/`w:br` 转换；拼接结果即段落文本。
- **预期输出**：数字末尾零、URL/邮箱不再因 run 边界丢失或插入空格（消除类型 1/7 与"伪重复"的共同源头）。
- **现状核查**：`python-docx` 的 `para.text` 本身按无空格拼接（`_get_element_text` 亦如此），**此项基本已达标**，保留为校验项，防止后续回归。

### P0-2 run 边界去重保守化（误删主源 #2）

- **文件**：`document_parser.py::apply_per_run_formatting`
- **输入**：`runs_data`（原文 run 数据）+ `translated_runs`（逐 run 译文）+ 保护集合
- **处理**：
  1. 完整 run 相等判定：`lower()` 折叠**改为大小写敏感**（`prev_stripped == curr_stripped`），且要求长度 ≥ 2 字符；
  2. 首尾词去重：**仅当重复词 ∈ 停用词表**时删除（`prev_words[-1] == curr_words[0]` 且为停用词）；专名/数字/实词一律跳过；
  3. 保留 spacing 修复逻辑（`need_space` 分支）。
- **预期输出**：`Cai Yuan` + `Cai Yuan`（真重复）仍清空后 run；`Software` + `Software Copyright`（跨 run 专名）完整保留；`软件` 拆 run 场景不再出现首词丢失。

### P0-3 短语 / 词级去重判定器替换（误删主源 #1）

- **文件**：`translator.py::_clean_segment` + 新增 `dedup_guard.py`
- **输入**：段内文本（URL 分段保护后非 URL 段）
- **处理**：删除 `(\S+(?:\s+\S+){0,6})\s+\1\b` 与 `\b(\w+)\s+\1\b`，替换为 `dedup_guard.dedup_text()`（L1 精确判定 + 三重门 + 白名单 + 审计）。保留：日期清理（`_DOUBLE_DATE_RE`/`_DUP_EN_DATE_RE`）、中文标点转换、字符级 `(\w)\1{2,}`、大小写边界/空格修复。
- **预期输出**：真重复按分级删除；伪重复（专名/补充说明/引用）保留；每次删除返回审计条目。

### P0-4 polish 模型指令收紧（误删主源 #3）

- **文件**：`translator.py::polish_text` system prompt
- **输入**：现有 proofread prompt
- **处理**：
  - "Duplicate / repeated words" 改为："**Remove only exact, immediately-adjacent duplicated words or phrases** (e.g. `indoor indoor`→`indoor`)."
  - 新增硬性禁令："**Never remove or merge any proper noun, number, technical term, institution name, or person name, even if it appears more than once in different locations. Never delete words that are not immediately adjacent duplicates.** Repeated terms at different positions are intentional and must be preserved."
- **预期输出**：模型不再执行语义去重；补充说明、语境差异、跨位置重复的专名全部保留。

### P1-1 保护集合构建（稀有词 / 专名词频统计）

- **文件**：`dedup_guard.py::build_protected_tokens` + `routes.py::_translate_paragraphs_sync`
- **输入**：全文档段落文本集合
- **处理**：统计词频，出现 ≤ 2 次的 token（排除停用词/数字/URL）加入保护集合；合并术语表 EN 侧词、拼音库人名。
- **预期输出**：`protected_tokens` 集合，随翻译循环传入 `_clean_mechanical_errors(text, protected)` 与 `apply_per_run_formatting`。

### P1-2 中英对照保真校验

- **文件**：`dedup_guard.py::verify_fidelity` + `routes.py::_translate_paragraphs_sync`
- **输入**：`(cn_text, en_text)` 对 + 术语表
- **处理**：数字完整性（含"万/亿"单位换算）、术语命中、专名出现检查；未命中 → warning 字符串。
- **预期输出**：每段 warnings 列表，汇入文件级质检报告。

### P2-1 删除审计与质检报告集成

- **文件**：`routes.py::_process_file_sync`、`_generate_quality_report`
- **输入**：翻译后最终文档文本
- **处理**：对最终文档全文跑 `dedup_guard.audit_duplicates`（只读检测，不修改），与 `verify_no_cn` 的 warnings 合并，生成质检报告；报告新增"去重审计"区块：被删片段、位置、置信度、中文对照。
- **预期输出**：任何自动删除均可追溯；审计发现中置信度告警 → 人工复核清单。

### P2-2 回归测试集落地（见第四章）

---

## 四、回归验证方法与验收标准

### 4.1 badcase 回归集构建

从 73 份译稿的 47 项问题抽取，分三组（测试脚本 `tests/test_dedup_regression.py`）：

**A 组：必须删除（真重复，badcase 类型 2，8 例）**

| 输入 | 期望输出 |
|---|---|
| `Copyright owner: Cai Yuan Cai Yuan` | `Copyright owner: Cai Yuan` |
| `Method of Right Acquisition: Original Acquisition Original Acquisition` | `Method of Right Acquisition: Original Acquisition` |
| `Software Copyright Registration No. 17558892 Software Copyright Registration No. 17558892` | `Software Copyright Registration No. 17558892` |
| `Chair of the Review Committee: Cai Yuan Cai Yuan` | `Chair of the Review Committee: Cai Yuan` |
| `Email: 227766088@qq. com 227766088@qq. com` | `Email: 227766088@qq. com` |
| `Client: Cai Yuan Cai Yuan` | `Client: Cai Yuan` |
| `Plastic Products V1.0 V1.0` | `Plastic Products V1.0` |
| `The report is objective, complete, and complete and true` | `The report is objective, complete, and complete and true`（模型级修复，去重器不动） |

**B 组：必须保留（伪重复 / 误删保护，6 例）**

| 输入 | 断言 |
|---|---|
| `Software Copyright Registration Certificate` + 跨 run `Software` / `Software Copyright` | 首词不丢 |
| `No. 2026 No. L-04077` | 两个 `No.` 均保留（非紧邻，告警不删） |
| 标题段 `Software Copyright Registration No. 17558892` + 正文段再次出现 | 两段均保留（跨段不判重） |
| 补充说明句 `He is the founder of the company, and he founded the company in 2010.` | 全文保留 |
| `Cai Cai`（单 token 实词重复） | 保留 + 告警 |
| `RMB 400,000 Yuan (¥400,000)` | 保留（非紧邻重复） |

**C 组：数字 / 术语保真（badcase 类型 1/6/8）**

| CN | EN | 断言 |
|---|---|---|
| 总价值一千零二十五万元人民币（1025.00万元） | RMB 10,250,000.00 | 数值 10,250,000 命中（万换算） |
| 证书号 软著登字第 17558889 号 | Software Copyright Registration No. 17558889 | 17558889 命中 |
| 邮编 325000 | Postal code 325000 | 325000 命中 |
| 蔡远 | Cai Yuan（术语表） | 术语命中 |

### 4.2 无重复断言（防漏删）

对输出文本跑 `audit_duplicates`：
- 紧邻精确重复（实词 ≥ 2 / 编号 / 停用词）→ **必须为 0**（硬门）；
- 单 token 实词重复 / L2 归一化重复 → 允许存在但必须出现在审计报告中（软门）。

### 4.3 无丢失断言（防误删）

对输出文本跑保真校验：
- **数字完整率**：CN 数字集合（含单位换算）⊆ EN 数值集合 → 100%（硬门）；
- **术语命中率**：术语表命中词标准译法全部出现 → 100%（硬门）；
- **专名保留率**：拼音库/术语表人名、机构名、证书号全部出现 → 100%（硬门）；
- **段落对齐**：CN 段落数 == EN 段落数，段内句子数误差 ≤ 1（软门）。

### 4.4 验收标准（门槛）

| 级别 | 验收项 | 门槛 |
|---|---|---|
| **硬** | A 组 8 例真重复 | 100% 正确删除，无残留 |
| **硬** | B 组 6 例伪重复 | 100% 保留，0 误删 |
| **硬** | C 组数字/术语保真 | 100% 命中 |
| **硬** | badcase 类型 1（数字截断） | 全部文档数字完整率 100%（校验拦截，人工确认后放行） |
| **硬** | 删除审计 | 每次自动删除有日志，人工抽检 100% 可解释 |
| **软** | badcase 类型 3/7（中文标点/URL） | ≥ 90% 修复 |
| **软** | badcase 类型 4/8（编号/术语一致性） | ≥ 90% 修复 |
| **防回归** | 修复后不得产生新的内容丢失 | 4.3 三项硬门全程通过 |

### 4.5 人工复核流程

1. 质检报告中的"中置信度告警"逐条人工确认（专名重复、大小写漂移、疑似截断）；
2. 按 10% 抽样比例做 CN↔EN 双语对照抽查；
3. 审计日志与抽查结果留档，作为下次迭代的回归基线。

---

## 五、本次代码改动记录（已实施，Windows/macOS 两版同步）

| 文件 | 改动 |
|---|---|
| `source/app/services/dedup_guard.py`（新增） | 判定器 `scan_exact_adjacent` / `dedup_text`、保护集合 `build_protected_tokens`、对照校验 `verify_fidelity` / `verify_terms`、审计 `audit_duplicates`、停用词表 |
| `source/app/services/translator.py` | `_clean_segment` 去重替换为判定器；`polish_text` prompt 收紧（仅删紧邻精确重复 + 专名/术语/非紧邻重复禁令）；`_clean_mechanical_errors` 支持 `protected` 透传 |
| `source/app/services/document_parser.py` | `apply_per_run_formatting` 去重保守化（完整 run 相等改大小写敏感；首尾词去重仅限停用词） |
| `source/app/api/routes.py` | 构建 `protected_tokens` 传入翻译循环；API 翻译段落跑 `verify_fidelity`/`verify_terms` 收集 warnings；质检报告集成重复审计 |
| `tests/test_dedup_regression.py`（新增） | 第四章回归集自动化测试，42 项断言全通过 |

**实施要点**（与第三章设计的差异确认）：
- `dedup_text` 采用**字符区间删除**（`start_del = 第一份末 token 结束位` → `end_del = 第二份末 token 结束位`），**不重建空白结构**——URL 分段保护依赖的段尾空格不会被吞掉（回归测试已固化该用例）。
- 停用词判定改为**原始 token 匹配**（`token.lower() in STOPWORDS`）：`No.`（Number 缩写）不再被 `no` 误判为停用词，`No. 17558892 No. 17558892` 按 2 实词精确重复正确删除。
- 单 token 内容词重复（`Cai Cai`、`think think`）默认**保留 + 告警**（保守方向，防专名误删）；高频词如需激进删除可后续放开配置。

**风险与回滚**：所有改动默认保留内容（保守方向），不改变函数签名主路径；若回归出现异常，可回滚 `translator.py` / `document_parser.py` 单文件即可恢复原行为。
